export class AssignaturesProfessor {

  ASSIGPROF_ASSIG_CODI!: string;
  ASSIGPROF_PROF_DNI!: string;

  constructor(
    ASSIGPROF_ASSIG_CODI: string,
    ASSIGPROF_PROF_DNI: string
  ) {
    this.ASSIGPROF_ASSIG_CODI = ASSIGPROF_ASSIG_CODI;
    this.ASSIGPROF_PROF_DNI = ASSIGPROF_PROF_DNI;
  }
}
